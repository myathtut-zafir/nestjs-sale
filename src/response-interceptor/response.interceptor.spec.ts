import { ResponseInterceptorInterceptor } from './response.interceptor';

describe('ResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseInterceptorInterceptor()).toBeDefined();
  });
});
